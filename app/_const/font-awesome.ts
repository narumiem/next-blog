import { config } from '@fortawesome/fontawesome-svg-core';
import '@fortawesome/fontawesome-svg-core/styles.css';

// Disable automatic CSS injection for Font Awesome icons.
config.autoAddCss = false;

/**
 * This file disables the automatic CSS injection for Font Awesome icons.
 * By setting `config.autoAddCss` to `false`, we prevent Font Awesome from injecting CSS stylesheets into the document.
 * This is useful when we want to manually handle the CSS integration ourselves.
 */
